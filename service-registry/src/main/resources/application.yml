spring:
  application:
    name: service-registry

server:
  port: 8761

eureka:
  instance:
    hostname: localhost # Address of Eureka Server
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false